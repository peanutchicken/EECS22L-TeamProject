# Installation Instructions
This file details how to install the chess program. 

1. Use tar to extract the files from the tarball:
    tar -xzvf P2_V2.0.tar.gz
2. Go to directory containing the files:
    cd P2_V2.0
3. Compile the program:
    make
4. The program should now be installed! Navigate to the bin/ directory in order to play the game, and then launch it with:
    ./gameClient (client)
    ./chessGame (server)
